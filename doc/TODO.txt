- partial отдаются модулю визуализации, но AI получает составной ход целиком
- Столбовые перемещения
- (symmetry (players West North East) (n e s w) (e s w n) (s w n e) (w n e s))
- При конфигурировании board сначала deletePosition, затем addLink
- В процессе игры конфигурация доски не изменяется, но в другой фазе (например при переключении переходов) игры может быть использована другая конфигурация (с автоматическим переносом фигур)
- Сканером транслировать DRF в SAX, преобразуемый в DOM
  - Структура
    - Все слова переводятся в теги
    - Для строк создается тег 'string', содержащий текст
    - Для атомов создается тег 'atom'
    - Для скобок без имени (в ZoG) создавать тег '_'
  - Можно использовать XPath для навигации
  - Можно использовать XSLT для преобразования (например ZRF в DRF)
- Добавить режим хода в DSG-нотацию (имя хода + параметры уникальны в пределах режима)
- Реализовать include с областями видимости (каталогами)
