; (+ a b)

GET a			; -- n
GET b			; -- n
+			; n n -- n

; (check (any dir))

GET dir			; -- l
ANY			; l -- n
LET dir			; n --
GET dir			; -- n
CHECK			; n --

; (if f a else b)

GET f			; -- ?
IF-NOT +2		; ? --
GET a			; -- n
JMP +1  		;
GET b			; -- n

; (while empty? dir)

GET empty?		; -- ?
IF-NOT +		; ? --
GET dir			; -- ?
JMP -4			;
